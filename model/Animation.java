package model;

/**
 * Defines the elements for an animation event on the
 * thread that is currently running
 */
public interface Animation {
    /**
     * Starts the animation
     */
    public void start();
}
